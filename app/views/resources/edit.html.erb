<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing resource</h1>

  <%= render "form", resource: @resource, game: nil, resource_group_options: @resource_group_options %>

  <%= render LinkButtonComponent.new text: "Show this resource", href: @resource %>
  <%= render LinkButtonComponent.new text: "Back to resources", href: game_resources_path(@resource.game) %>
</div>
