<%= form_with(model: @room_resource, class: "contents") do |form| %>
  <% if @room_resource.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(@room_resource.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% @room_resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= render "shared/number_field", f: form, field: :amount %>
  </div>

  <div class="inline">
    <%= form.submit "Save", class: "btn-primary" %>
  </div>
<% end %>
