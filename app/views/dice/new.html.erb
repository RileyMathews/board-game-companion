<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New die</h1>

  <%= render "form", die: @die, game: @game %>

  <%= render "shared/link_button", text: 'Back to dice', path: game_dice_path(@game) %>
</div>
