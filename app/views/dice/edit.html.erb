<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing die</h1>

  <%= render "form", die: @die, game: nil %>

  <%= render "shared/link_button", text: "Show this die", path: @die %>
  <%= render "shared/link_button", text: "Back to dice", path: game_dice_path(@die.game) %>
</div>
