<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing die</h1>

  <%= render "form", die: @die, game: nil %>

  <%= link_to "Show this die", @die, class: "btn-light" %>
  <%= link_to "Back to dice", game_dice_path(@die.game), class: "btn-light" %>
</div>
