<%= turbo_frame_tag die do %>
<div>
  <div class="flex items-center justify-between py-2">
      <p><%= die.name %></p>
      <div class="flex gap-2">
        <%= link_to "Edit", edit_die_path(die), class: "btn" %>
        <%= button_to "Delete", die_path(die), method: :delete, class: "btn" %>
      </div>
  </div>
  <hr>
</div>
<% end %>
