<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Games</h1>
    <%= render LinkButtonComponent.new text: 'New game', href: new_game_path %>
  </div>

  <div id="games" class="min-w-full">
    <%= render @games %>
  </div>
</div>
