<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Games</h1>
    <%= render "shared/link_button", text: 'New game', path: new_game_path %>
  </div>

  <div id="games" class="min-w-full">
    <%= render @games %>
  </div>
</div>
