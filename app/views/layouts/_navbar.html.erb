<nav class="border-b-2 border-slate-900 p-2">
    <% if current_user %>
        <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
    <% else %>
        <%= link_to 'Login', new_user_session_path, { class: "text-blue-500 underline" } %>
    <% end %>
</nav>