<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing room</h1>

  <%= render "form", room: @room, game: nil %>

  <%= link_to "Show this room", @room, class: "btn-light" %>
  <%= link_to "Back to rooms", game_rooms_path(@room.game), class: "btn-light" %>
</div>
