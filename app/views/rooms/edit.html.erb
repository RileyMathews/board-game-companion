<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing room</h1>

  <%= render "form", room: @room, game: nil %>

  <%= render LinkButtonComponent.new text: "Show this room", href: @room %>
  <%= render LinkButtonComponent.new text: "Back to rooms", href: game_rooms_path(@room.game) %>
</div>
