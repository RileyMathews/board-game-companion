<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Creating room for <%= @game.name %></h1>

  <%= render "form", room: @room, game: @game %>

  <%= link_to 'Back to rooms', game_rooms_path(@game), class: "btn-light" %>
</div>
