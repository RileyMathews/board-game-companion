<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New room</h1>

  <%= render "form", room: @room, game: @game %>

  <%= render "shared/link_button", text: 'Back to rooms', path: game_rooms_path(@game) %>
</div>
