<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Rooms for <%= @game.name %></h1>
    <%= render "shared/link_button", text: 'New room', path: new_game_room_path(@game) %>
  </div>

  <div id="rooms" class="min-w-full">
    <%= render @rooms %>
  </div>
</div>
