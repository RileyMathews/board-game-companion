<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing face</h1>

  <%= render "form", face: @face, die: nil %>

  <%= link_to "Show this face", @face, class: "btn-light" %>
  <%= link_to "Back to faces", die_faces_path(@face.die), class: "btn-light" %>
</div>
