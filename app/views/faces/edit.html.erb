<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing face</h1>

  <%= render "form", face: @face, die: nil %>

  <%= render "shared/link_button", text: "Show this face", path: @face %>
  <%= render "shared/link_button", text: "Back to faces", path: die_faces_path(@face.die) %>
</div>
