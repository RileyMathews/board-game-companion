<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing face</h1>

  <%= render "form", face: @face, die: nil %>

  <%= render LinkButtonComponent.new text: "Show this face", href: @face %>
  <%= render LinkButtonComponent.new text: "Back to faces", href: die_faces_path(@face.die) %>
</div>
