<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New face</h1>

  <%= render "form", face: @face, die: @die %>

  <%= render LinkButtonComponent.new text: 'Back to faces', href: die_faces_path(@die) %>
</div>
